apiVersion: v1
kind: Secret
metadata:
  name: {{ include "nikogrid.fullname" . }}-secret
  annotations:
    "helm.sh/hook": "pre-install"
    "helm.sh/hook-delete-policy": "before-hook-creation"
type: Opaque
data:
  tokenSecret: {{ randAlphaNum 32 | b64enc | quote }}
